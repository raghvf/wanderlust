# use-mapbox-gl-js-with-vanilla-js

A simple Mapbox GL JS web map using Vanilla JavaScript and Vite as a bundler.

## Prerequisites

- Node v18.20 or higher
- npm

## How to run

- Install dependencies `npm install`
- Run the development server `npm run dev`

